public class StringLength {
     public static int getStringLength(String s) {
        int str_len = 0;
        while (str_len <  s.length()) {
            str_len ++;
        }
        return str_len;
    }
}
